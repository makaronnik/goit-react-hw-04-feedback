import styled from 'styled-components';

const SectionStyled = styled.section`
  padding: 10px 40px;

  &:first-child {
    margin-top: 20px;
  }

  & h1 {
    margin-top: 0;

    font-weight: 600;
    font-size: 2em;
  }
`;

export default SectionStyled;
